# openspec/config.yaml
# Auto-generated by sdd-init — 2026-05-30
# Do not edit manually unless you understand the full SDD config contract.

schema: spec-driven

context: |
  Project: Contreras Martínez Arquitectura Integral (cm)
  Tech stack: Vanilla HTML5, CSS3 (custom properties), JavaScript (ES6+)
  Architecture: Static multi-page website (index + 3 project pages + 1 template), no SPA router, no build step
  Design: Editorial style — DM Sans (body) + Urbanist (headings), paper bg (#fbfaf7), CSS Grid layouts, clamp() responsive type
  Reusable patterns: .media-block (bg-image + grid overlay), .text-link (underline + arrow), .eyebrow (label uppercase), service-row, philosophy-card, portfolio-card, accordion (details/summary)
  JS: IntersectionObserver scroll reveals, mobile hamburger menu, accordion dedup, Formspree form handler
  Testing: No testing infrastructure detected
  Lang: Spanish (Chile)
  Known issues: (1) All project images use same placeholder (Proyecto01.png), (2) Header/footer duplicated across 5 HTML files, (3) 5 orphaned backup HTML files in root, (4) Social links point to "#!", (5) JSON-LD only on index.html, (6) Recursos/Proyectos/ empty, (7) material.png + material.webp duplicate
  SEO: FAQPage JSON-LD structured data on index.html only
  Form: Formspree endpoint (https://formspree.io/f/xdkebogw), validation in JS
  Git: github.com/ColiflorFU/CM, v1.0, REGISTRO-CAMBIOS.md with multi-machine workflow

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (vanilla JS, no frameworks)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner: none
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false

changes: []
